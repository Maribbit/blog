---
// Headphone — click to open Apple Music playlist overlay.
import hpSvg from '../assets/svg/props/Headphone.svg?raw';
import SvgAsset from './SvgAsset.astro';
---

<div class="headphone" data-nav="music" role="button" tabindex="0" aria-label="打开歌单">
  <SvgAsset content={hpSvg} />
</div>

<style>
  .headphone {
    position: absolute;
    left: 40%;
    top: 13%;
    width: calc(22 * var(--cm));
    height: calc(22 * var(--cm) * 689 / 418);
    transform: translate(-50%, -50%) rotate(-30deg);
    z-index: 27;
    cursor: pointer;
    transition: top 0.25s ease, transform 0.25s ease;
  }

  .headphone:hover,
  .headphone:focus-visible {
    top: calc(13% - 12px);
    transform: translate(-50%, -50%) rotate(-30deg);
  }

  .headphone :global(svg) {
    width: 100%;
    height: 100%;
    display: block;
  }

  .headphone:focus-visible {
    outline: none;
  }
</style>
